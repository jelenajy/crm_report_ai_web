# Changelog

## 2026-09-21 · Premium Web UI (V1.1)

- 发布复核：知识缺失与反馈文案明确只在本页临时标记，不再暗示已向负责人提交；补齐暖金圆形发送按钮、短暂闪耀的八角北极星、知识治理页脚及可直接取用的同步 HTML 副本。
- 建立 Consumer Hive / C Hive Sage / 指标顾问三级品牌层级。
- 上线深石墨与暖象牙的黑金工作台、无硬框八角北极星及 3.6 秒低频动效；支持减少动态效果偏好。
- 统一新建对话、报表触发器与输入区的柔和圆角样式，并更新欢迎标语与说明。
- 使用定制圆角 listbox 替换原生报表下拉弹层，支持鼠标、键盘、外部点击关闭与移动端焦点管理。
- 使用 `applyReportSelection` 统一报表菜单和跨报表推荐卡；同步标题、知识包、状态、输入提示与快捷问题。
- 将“查看知识依据”升级为可访问的展开与收起交互，箭头、ARIA 和内容可见性同步；历史回答保留提问时快照。
- 清理用户可见的“原型”和“模拟”类文案；文档继续披露当前无真实 AI、后台记录、反馈落库和会话持久化。
- 更新需求与实现映射、设计说明、README 和一致性校验，保留四类路由与隔离式本地服务验证。

## V1.0 Web — 2026-09-20

- 初始化独立的 C Hive Sage｜指标顾问 Web 工作区。
- 建立设计规格、README、本地启动说明和原型边界。
- 从嵌入式报表侧边交互迁移为独立的单一 Web 工作台，并移除报表图像底图依赖。
- 增加左侧导航、五报表路由、移动端覆盖式侧栏及 Escape/遮罩关闭交互。
- 迁移 Customer Type 模拟问答、四类问题分流、知识依据、追问、反馈和模拟会话逻辑。
- 新增 `verify_web_prototype.py` 与 `verify_requirements_sync.py` 两类自动校验，并补齐需求和 HTML 行为映射。
- 强化需求同步校验为 requirement ID、函数、DOM 与实现行为的双向结构化契约；所有正常回答的知识依据标题现固定包含提问时的报表快照。
- 补全报表配置字段、四类回答可见结构及移动侧栏 Escape/遮罩/焦点链的静态契约证据；需求映射明确该校验不替代浏览器运行时验收。
- 修复中文 IME Enter 误发送、输入焦点不可见、跨报表快捷问题路由漂移，以及回答异常后控件无法恢复的问题。
- 补齐知识依据唯一 `aria-controls`、反馈 `aria-pressed`、移动遮罩隐藏状态、小字号对比度和加载期间“新建对话”禁用状态。
- 取消快速关闭移动侧栏时尚未执行的延迟聚焦，避免焦点进入已隐藏侧栏。
- 本地启动改为仅绑定 `127.0.0.1` 并从临时干净目录发布，避免暴露 `.git` 等仓库文件；README 文件职责同步为已交付状态。
- 新增最终审查静态回归和无第三方测试依赖的 Chrome/Chromium 浏览器回归，覆盖配置路由、IME、计算焦点样式、故障注入恢复和动态 ARIA。
